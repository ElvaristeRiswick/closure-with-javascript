/*function greeding(greet) {
  return function (name) {
    console.log(`${greet} ${name}`);
  };
}

let greedingSon = greeding("hi");
greedingSon ("djimelove");

greeding();

const countNumber = function () {
  let count = 0;
  return function () {
    count++;
    console.log(`${count} number`);
  };
};

let counter = countNumber();
counter();
counter();
counter();*/
